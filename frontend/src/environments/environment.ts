// Development defaults, used by `ng serve`. Production values are supplied
// via fileReplacements in angular.json (environment.prod.ts).
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8080/api/v1',
};
